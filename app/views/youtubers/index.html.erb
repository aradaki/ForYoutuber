<!--タグリスト-->
<% @tag_list.each do |list|%>
<%=list.name %>
<%="(#{list.posts.count})" %><% end %>

投稿一覧画面で投稿に紐づくタグを表示
<% @youtubers.each do |youtuber| %>
<i class="fas fa-tag"><%= youtuber.tags.map(&:name).join(', ') %></i>